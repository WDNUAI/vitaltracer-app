name: vitaltracer_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter_blue_plus: ^1.32.0
  flutter_bluetooth_serial: ^0.4.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  settings_ui: ^2.0.2
  provider: ^6.0.0
  flutter_screenutil: ^5.0.0


  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:

    - lib/images/

 


